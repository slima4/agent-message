# Pin the pre-0.16 default rule set. CI runs unpinned `pipx run ruff`; 0.16 changed
# defaults (drops E401/E701/E702, adds I001/RUF100) which breaks the intentional
# dense one-liner style (see file-level noqa in bin/agent-message-cmd).
lint.select = ["E4", "E7", "E9", "F"]
